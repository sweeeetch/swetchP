import React from "react";
//import "./FiltersGallery.css";
import "./FiltersGalleryAnimation.js";
import video24 from "../videos/video24.mp4";

const FiltersGallery = () => {
  return (
    <section>
      {/* <div id="video-gallery" class="video-gallery">
        <div className="gallery1">
          <div className="container">
            {[...Array(10).keys()].map((index) => (
              <div key={index} className="video1 ui-card">
                <video src={video24} controls className="video1"></video>
              </div>
            ))}
          </div>
        </div>
      </div> */}
      <h1>portfolio</h1>
    </section>
  );
};

export default FiltersGallery;
